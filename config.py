class Config:
    SECRET_KEY = 'secret'  # Change this to a real secret key for production
    DATABASE_URI = 'postgresql://postgres:nawawi@localhost/SSIS'  # Update credentials

# You can add more configuration classes for different environments (development, testing, production)
